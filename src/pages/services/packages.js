import Head from "next/head";

const packages = () => {
    return (
        <div>
            <Head>
                <title> Package || Check Package </title>
            </Head>
            <h2 className='text-3xl' > Cleaning Service Offer</h2>
        </div>
    );
};

export default packages;